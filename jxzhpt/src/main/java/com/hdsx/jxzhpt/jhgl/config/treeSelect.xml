<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "/xsd/mybatis-3-mapper.dtd">
<mapper namespace="com.hdsx.jxzhpt.treenode">
	<select id="queryGydwByParent" parameterType="string" resultType="treenode">
		select id ,name as text,t.parent from XTGL_DEPARTMENT t 
		where
		<if test="_parameter==null">
			t.parent is null
		</if>
		<if test="_parameter!=null">
			t.parent = #{_parameter}
		</if>
	</select>
	<select id="queryChildCount" parameterType="string" resultType="int">
		select count(id) from XTGL_DEPARTMENT t where
		<if test="_parameter==null">
			t.parent is null
		</if>
		<if test="_parameter!=null">
			t.parent = #{_parameter}
		</if>
	</select>
	<select id="queryXzqhByParent" parameterType="string" resultType="treenode">
		select id ,name as text,t.parent from XTGL_XZQH t 
		where
		<if test="_parameter==null">
			t.parent is null
		</if>
		<if test="_parameter!=null">
			t.parent = #{_parameter}
		</if>
	</select>
</mapper>